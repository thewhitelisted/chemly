# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# HTTP client
httpx==0.25.2

# Chemistry libraries
stout-pypi==2.0.5
pubchempy==1.0.4

# PyTorch for optimizations (CPU version by default, GPU if available)
torch>=2.0.0
# If you have GPU, you can specify CUDA version:
# torch>=2.0.0+cu118 --index-url https://download.pytorch.org/whl/cu118

# Performance optimizations for uvicorn
uvloop==0.19.0
httptools==0.6.1

# Environment handling
python-dotenv==1.0.0

# Optional: Memory profiling (useful for optimization)
psutil==5.9.6 